'use strict';
//var dataProvider = require('../../data/api/reports.js');
/**
 * Operations on /api/reports
 */
module.exports = {
    /**
     * summary: Get reports
     * description: Retrieve various reports.
     * parameters: 
     * produces: 
     * responses: 200
     */
    get: function (req, res, next) {
        /**
         * Get the data for response 200
         * For response `default` status 200 is used.
         */
     
            res.send('hello');
        }
    };

